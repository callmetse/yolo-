# 站房感知项目入口

更新：2026-09-19。实际项目根目录为 `D:\Projects`。

## 当前主计划

[人员识别、数据采集与交付推进计划](station-device/project.md) 是下一阶段的主计划，包含当前代码核查、多人检测调优顺序、数据字段、统计口径、图表与接口交接、阶段验收。

| 项目 | 当前用途 | 下一步 |
|---|---|---|
| `people_counter` | 已有录像检测、每帧人数 CSV、人数曲线；作为视觉验证区 | 统一画框和计数配置，建立人工对照，比较参数与模型 |
| `station-device` | 装置集成主仓库；已有配置和模拟环境数据示例 | 接入经验证的视觉输出，增加记录、汇总、导出与通信 |

立即执行顺序：

1. 固定视频、ROI 和人工计数口径，抽取 100–200 帧做起步对照。
2. 用同一批帧比较 YOLO11n/s、输入尺寸和置信度；分别记录误差与速度。
3. 新增每次运行独立目录和 manifest，保存视频来源、实际模型、参数及时间依据。
4. 在 `station-device` 完成原始记录 → 1 分钟汇总 → CSV/JSON + 图表 + 数据字典。
5. 接收方确认字段后，做 HTTP 本地接收、断网补传，再移植树莓派。

本次仅核查并更新计划文档；未修改推理脚本、训练模型、安装依赖或重跑现有检测数据。各阶段尚待实施与验收。
