# 项目依赖清单。装法：python -m pip install -r requirements.txt
#
# 只有画图要用的第三方库需要写在这里。
# csv / datetime / math / random / time / pathlib 都是 Python 自带的标准库，
# 不用装、也不用写进这个文件，直接 import 就能用。

matplotlib==3.9.2

# 任务2/3 要用到的，已在 2026-09-19 装好（版本就是当前 .venv 里的）：
opencv-python==5.0.0.93    # 读取视频、逐帧处理、写结果视频
ultralytics==8.4.155       # YOLO11n 人员检测（依赖 torch，见下面一段）
# pandas                   ← 可选：更方便地处理表格数据，用到再取消注释
#
# 【显卡（GPU）】2026-09-21 已把 torch 换成 CUDA 版，推理走本机 RTX 3050。
# 当前实际装的是 torch==2.14.0+cu126 与 torchvision==0.29.0+cu126（不再是 CPU 版）。
# 重建环境时要注意：PyPI 上的 torch 是 CPU 版，CUDA 版必须从 PyTorch 官方源单独装：
#   NO_PROXY="*" no_proxy="*" ./.venv/Scripts/python.exe -m pip install \
#     "torch==2.14.0+cu126" "torchvision==0.29.0+cu126" \
#     --index-url https://download.pytorch.org/whl/cu126
# 为什么选 cu126、怎么验证、怎么退回 CPU 版、踩过的代理坑：见 环境变更记录.md
