# 项目依赖清单。装法：python -m pip install -r requirements.txt
#
# 只有画图要用的第三方库需要写在这里。
# csv / datetime / math / random / time / pathlib 都是 Python 自带的标准库，
# 不用装、也不用写进这个文件，直接 import 就能用。

matplotlib==3.9.2

# 任务2/3 要用到的，已在 2026-09-19 装好（版本就是当前 .venv 里的）：
opencv-python==5.0.0.93    # 读取视频、逐帧处理、写结果视频
ultralytics==8.4.155       # YOLO11n 人员检测（会自动带上 CPU 版 PyTorch）
# pandas                   ← 可选：更方便地处理表格数据，用到再取消注释
#
# 【可选升级：用显卡跑】上面装的是 CPU 版 torch，本机 RTX 3050 还没用上。
# 想切到 GPU（快好几倍），执行下面两条（共约 2.5 GB，装完重跑 step4 即可）：
#   ./.venv/Scripts/python.exe -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   ./.venv/Scripts/python.exe -c "import torch; print(torch.cuda.is_available())"   # 应输出 True
